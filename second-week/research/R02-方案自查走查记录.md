# 第二周方案自查走查记录

记录日期：2026-08-24  
走查对象：第二周产品方案（候选场景定义、主线演示案例、故障分类口径）  
执行人：本人自查，无外部评审人参与  
方式：对抗式自查——先刻意站到质疑者立场找方案的结构性漏洞，再站回方案方立场逐条检验；成立的改，不成立的写明理由，判不了的悬置

这是一次自查，不是他人评审。下文「质疑」与「检验」两侧都是我本人写的，分开记录是为了让检验过程可追溯、避免自己给自己放水，不代表存在第二位参与者。承认的部分和驳回的部分都如实收录：不写成单方面认错，也不写成单方面辩护。

## 质疑一：「跨 Agent」概念不清

### 质疑

候选场景里的「跨 Agent 因果故障定位」没有说明「跨 Agent」指什么：是「一个主 Agent 带多个子 Agent」，还是「多个独立 Agent」？Agent 以什么形式存在、彼此怎么通信，方案里都没有交代。概念边界不清，后续的数据模型和诊断逻辑就没有落点。

### 检验

质疑成立，定义确实缺失。当场补齐口径：

- **两种拓扑都算**。关键特征不是拓扑形状，而是「报错的执行体与产生根因的执行体不是同一个」。主从结构、对等结构都会出现这个特征。
- **存在形式三种**：同进程内的框架节点（LangGraph 节点、Agent SDK 子代理）；独立部署的 Agent 服务；混合形态——远程 Agent 经 MCP 或 A2A 协议被当作一个「工具」调用，调用方甚至不知道对面是个 Agent。
- **通信四条通道**：交接（Handoff）、同步调用、异步消息、共享状态与记忆。其中共享记忆最隐蔽——A 写入、B 读取，调用树上看不出任何依赖关系，必须建因果读写边（谁写了这个键、谁后来读了它）才能发现。

### 结论

上述定义作为「跨 Agent」的统一口径写入事实源文档，候选场景描述同步更新。此项无保留意见。

## 质疑二：主线案例「状态与记忆过期」是「照着答案写原因」

### 质疑

这是本轮最严重的问题。FM-01（状态与记忆过期）看起来不是从真实需求里长出来的，而是先有了「要演示因果诊断」这个答案，再倒推出一个方便画图的故障：

1. 该案例有成本极低的解法——给快照加 TTL、订阅变更事件。一行配置能解决的问题，撑不起一个诊断产品的价值主张。
2. 成熟 Agent 系统已在每层部署了同类防线。以 Claude Code 为例：写前校验 live 状态（Edit 的 old_string 匹配失败即拒绝）、文件变更通知、上下文里显式标注快照时效。防线密到这类故障很难发生。

### 检验

**成立的部分**：案例选择确实是演示便利性驱动，不是需求强度驱动，这一点站不住。六类故障里「状态过期」是最容易画图讲清、但最不能代表产品价值的一类。主线案例必须换。

**不成立的部分**（逐条陈述理由）：

- (a) **类别错误**。Claude Code 的防线是 Anthropic 为编码场景手写进 harness 的，不是 Agent 运行时的通用能力。用 Agent SDK 自建业务 Agent 时，记忆层、新鲜度校验全要自己实现。「用 Claude Code 做订单助手就不会有这问题」混淆了两类东西——Claude Code 是成品应用，不是构建业务 Agent 的运行时。
- (b) **防线迁移不过去，因为真值源性质不同**。文件在本地、读取免费、读完即最新，所以「写前重读一次」成本为零；订单状态在远端，有延迟、限流和调用费用，「每次查 live」在业务场景是要付账的，团队必然做快照缓存，过期窗口必然存在。
- (c) **即使装齐防线，故障只是换了形态**。Claude Code 的 Edit 拒绝本身也是一次失败：它告诉你 old_string 没匹配上，但不会告诉你「40 轮前读的文件在此期间被别的进程改过」。防线把静默错误变成显式报错，归因这一步依然要人往回翻。

**两侧都成立的事实**（不依赖任何特定故障类型）：Agent 是多步骤的，报错步骤与根因步骤经常不是同一个；现有工具（Langfuse、LangSmith 一类）只能把几百个 Span 摊开给人看。产品价值锚在这句话上，而不是锚在某一类故障上。

### 结论

- 主线演示案例从 FM-01 切换到无低成本解法的故障类型；FM-01 降级为普通事故保留，不再承担价值论证。
- 新增故障类型的筛选标准：执行层全成功（HTTP 全 200、规则引擎判不出异常）、故障由业务侧发现（用户投诉 / Judge 评分 / 对账）、根因是复合条件、证据必须跨步骤比对才能成立。满足这四条的故障，「加一行配置」解决不了，人工排查也没有捷径，才是诊断产品该吃的场景。

### 自查未能判定的部分

对「Claude Code 式防线能否证明该类故障不值得诊断」，自查没能得出结论：质疑侧认为防线的存在至少说明头部团队会自行消化这类问题；检验侧认为防线只覆盖编码场景且只改变故障形态（见上文 a/b/c）。这是自查的固有局限——两侧都是我本人的判断，缺少外部信息无法定论。此项不影响处理动作（主线案例切换后该争论不再影响产品主张），故悬置，留待两类外部输入验证：第一周记录的 AgentLoop 等编码 Agent 审计类产品的市场表现，以及后续从产品负责人处获取的内部真实事故形态。

## 处理动作（已完成）

1. 新增三类「无低成本解法」的故障类型：
   - **FM-07 检索排序漂移**：检索全部成功，但排序权重被上游配置变更悄悄改掉，答案质量下滑由 Judge 与投诉发现。
   - **FM-08 多轮上下文冲突**：多轮对话中先后注入的两段上下文互相矛盾，模型采信了错的那段，单步看每一步都合理。
   - **FM-09 重试状态放大**：重试成功掩盖了首次执行留下的副作用，副作用被后续步骤消费并放大。
   - 三类共同结构：执行层全成功、业务侧发现、复合条件根因、证据跨步骤比对。
2. 主线演示案例切换为 FM-07（事故 INC-2026-0821-017）；FM-01 降为普通事故保留在事故列表中。
3. 建立唯一事实源 `docs/00-故障分类与案例事实源.md`，所有文档与原型的故障分类、事故编号、案例细节统一以此为准，消除多处口径漂移的可能。
4. 目标 Agent 定位写清：不是编码 Agent，而是用 LangGraph / Dify / Coze / Agent SDK 搭建的生产业务 Agent（客服、工单、财务审批、数据分析），运维方是平台团队与 SRE，规模前提是每天几千到几十万次执行。编码 Agent（Claude Code / Cursor / Codex）是潜在数据源而非竞品——第一周报告已记录 AgentLoop 在做这类行为审计。

## 处理闭环

| 自查发现 | 处理动作 | 涉及文件 | 状态 |
|---|---|---|---|
| 「跨 Agent」定义缺失 | 补齐口径：核心特征（报错执行体 ≠ 根因执行体）、三种存在形式、四条通信通道，共享记忆需因果读写边 | [docs/00-故障分类与案例事实源.md](../docs/00-故障分类与案例事实源.md) | 已完成 |
| FM-01 撑不起价值主张 | 新增 FM-07 / FM-08 / FM-09 三类无低成本解法的故障，明确四条筛选标准 | [docs/00-故障分类与案例事实源.md](../docs/00-故障分类与案例事实源.md) | 已完成 |
| 主线案例是演示便利性驱动 | 主线演示切换为 FM-07（INC-2026-0821-017），FM-01 降为普通事故 | [docs/00-故障分类与案例事实源.md](../docs/00-故障分类与案例事实源.md)、[prototype/index.html](../prototype/index.html) | 已完成 |
| 多处文档口径不一致的风险 | 建立唯一事实源，全部文档与原型以 docs/00 为准 | [docs/00-故障分类与案例事实源.md](../docs/00-故障分类与案例事实源.md) | 已完成 |
| 目标用户与编码 Agent 的关系不清 | 定位写清：生产业务 Agent 的平台团队与 SRE；编码 Agent 是数据源而非竞品 | [docs/00-故障分类与案例事实源.md](../docs/00-故障分类与案例事实源.md)、[docs/03-产品方案说明.md](../docs/03-产品方案说明.md) | 已完成 |
| Claude Code 防线是否证明故障不值得诊断 | 两侧观点均如实记录（见「自查未能判定的部分」），主线切换后不影响产品主张 | 本文件 | 悬置，待外部输入 |

## 本轮可以下的结论

- 两个发现一个指向概念严谨性，一个指向价值主张的根基，均属方案级问题而非表述问题。自查能挖到方案级问题，说明对抗式自查这个方法本身有效。
- 质疑二迫使案例筛选标准从「好画图」换成「无低成本解法 + 执行层全成功 + 业务侧发现」，这是本轮最大的方案改进。
- 把驳回的部分也如实写下，是为了下次走查可以接着往下推，而不是每轮从零开始。
- **本轮的方法边界**：全部输入来自竞品资料与公开材料的推演，无内部真实事故样本，也无第二人视角。自查能发现逻辑漏洞，但无法验证「这类故障在真实业务里到底有多常见」。这个短板需要外部输入补齐，已列入待验证问题清单。
