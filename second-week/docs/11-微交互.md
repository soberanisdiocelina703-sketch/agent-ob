> 提交状态：已完成
> 核心交付：微交互节点、反馈规则和减弱动效策略
> 上游依据：[项目索引](../评分索引.md) · 下游证据：[原型](../prototype/index.html)

# 任务 11：微交互 / 情感化设计

设计目标只有一个：**让用户信任诊断结论**。所有动效服务于「确定性感」与「克制感」，不做装饰性动画。全部动效已在原型 v1.8 中用 CSS/JS 实现（实现位置见下表），时长 150–400ms（例外：异步分析的加载态，以及证据定位闪烁 1.6s、新用例行背景闪淡 1.2s 两处驻留提示），不阻塞任何操作，并通过 `prefers-reduced-motion` 尊重系统减弱动效设置。

### 11.1 品牌语气与视觉原则

> **像一位可靠的资深工程师：不夸张、不卖萌、每句话可验证。**

- **文案三规则：** ①成功文案说明「系统做了什么 + 对用户意味着什么」，不做庆祝式表达；②错误或降级文案必须带恢复路径；③空状态给出可执行引导，不用主题化插画填充。
- **视觉方向「标准后台基线 + 诊断信息增强」（v1.8）：** 采用工程平台常见的浅灰背景、白色卡片、左侧导航和品牌蓝主操作；标题与正文统一使用无衬线字体，数据保留等宽字体。产品差异通过信息组织体现：首故障点清晰标记、证据引用可回跳、支持与反面证据并列。移除叙事性主题元素和衬线公文标题。
- **使用创新的边界：** 不创造新的角色或仪式，不让用户学习额外隐喻。新设计必须直接缩短核对路径、降低误判，且能够套用现有的列表—详情—确认操作习惯。
- **色彩语义（全站一致）：**

| 色 | 语义 | 使用处 |
|---|---|---|
| 品牌蓝 `#2563eb` | 可执行动作与当前选中项 | 主按钮、链接、导航激活项、事件定位 |
| 规则青 `#0d9488` | 确定性规则与支持证据 | 规则标签、支持证据边条 |
| 成功绿 `#16a34a` | 已确认 / 通过 | 确认按钮、通过标签、完成状态 |
| 警示琥珀 `#b45309` | 反面证据 / 警告 / 待处理 | 反面证据、门禁警告、首故障点 |
| 错误红 `#dc2626` | 报错事实或危险操作 | 报错点、错误码、排除操作边框 |
| 模型紫 `#7c3aed` | 模型推断，提示不确定性 | 模型标签、语义分析加载态 |

- **图标风格：** 使用常见的搜索、对比、勾选、警告图标编码功能；不使用带角色暗示的叙事性符号。
### 11.2 关键情感节点（6 个，统一结构：触发时机 → 动效形式 → 情绪与目的）

| # | 节点 | 触发时机 | 动效/反馈形式（时长、缓动） | 传达的情绪与目的 | 原型实现 |
|---|---|---|---|---|---|
| 1 | **诊断加载中** | 从事故列表首次进入工作台 | 规则候选立即呈现；虚线框加载条 + 紫色 spinner「规则判定已返回 · 语义分析中…」，约 1.1s 后模型候选以 350ms 渐入追加，toast 告知 | 「确定的部分已经给你了，不确定的部分我不装快」——把 8.4 节「规则先行、模型异步」的技术时序变成可感知的诚实感 | `#diagload`、`.cand.fadein`、`openIncident()` |
| 2 | **确认根因** | 点击「确认是根因」 | 操作区隐去，结果条 350ms `settle` 入场；标准圆形对勾与完成文案同步出现 | 清晰说明结果已保存、写入何处以及下一步，不制造额外仪式感 | `.status-check`、`.vd-done.show`、`submitReview()` |
| 3 | **排除候选原因** | 点击「排除该原因」 | 仅 200ms 弹窗上滑入场请求理由；无红色闪烁、无惩罚性动效 | **克制与严肃**：驳回 AI 是正常且有价值的行为，不该有「出错了」的情绪暗示；理由必选的文案解释「为什么要多问一步」 | `.modal` 动画、`m-reject` 弹窗 |
| 4 | **回归用例生成成功** | 转用例弹窗点击「生成用例」 | 跳转门禁页，新用例行 300ms 左滑入 + 1.2s 背景闪淡定格为品牌蓝浅底；toast 陈述结果并交代下一步：「已生成回归用例 REG-1041（待复核）——点击该行复核不变量，确认生效后才参与门禁」 | **成就感但不打断**：闭环终点值得一次确认，但用高亮定格代替庆祝弹窗——防线增加了，这是日常工作不是节日；文案同时把「待复核」的下一步说清楚，不让用户误以为已经生效 | `.caseNew`、`@keyframes rowIn/rowFlash`、`createCase()` / `approveCase()` |
| 5 | **门禁警告** | 进入门禁页 | 「警告」大字单次 400ms 脉冲（放大 1.12 倍回落），不循环、不闪烁 | **克制提醒而非警报**：警告默认不阻断（决策 D-5），动效强度与之匹配——提请注意，不制造恐慌 | `#gwarn`、`.pulse`、`go()` |
| 6 | **空状态与首次引导** | 首次打开原型 / 新项目无数据 | 引导横幅 300ms 下滑入场；步骤编号可点击直达对应页面；空状态文案给三步接入引导（U-4） | **被引路而非被说教**：第一次打开就知道往哪走；引导可随时关闭，不劫持注意力 | `.guide` 动画、`<em onclick>` |

### 11.3 全局微交互基线

页面切换 180ms 淡入上移（连续感，不闪白）；按钮按压 100ms 缩放至 97%（物理反馈）；toast 250ms 上滑入场、2.8s 自动消失（所有 toast 文案都是「事实陈述」语气）；弹窗 200ms 缩放入场；键盘焦点使用品牌蓝描边（`:focus-visible`）。以上均为 CSS 实现、可被 `prefers-reduced-motion` 一键关闭。

---

## 交付闭环

| 检查项 | 结果 |
|---|---|
| 本任务产出 | 微交互节点、反馈规则和减弱动效策略 |
| 核验方式 | CSS/JS 实现项检查；prefers-reduced-motion 已配置 |
| 当前状态 | 已完成 |
| 剩余风险 | 动效需在真实设备上复核可读性 |
