> 提交状态：已完成
> 核心交付：正向、异常、边界和诊断质量测试集
> 上游依据：[项目索引](../评分索引.md) · 下游证据：[原型](../prototype/index.html)

# 任务 9：测试用例设计

优先级说明：P0 = 阻塞发布；P1 = 影响核心体验；P2 = 增强项。

### 9.1 核心流程（正向路径）

| 编号 | 模块 | 前置条件 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-01 | M1 | 新项目，测试 Agent 已接入 | 按向导接入 LangGraph 适配器并运行一次 | 5 分钟内体检报告生成，四项均有得分 | P0 |
| TC-02A | M2A | 已采集成功与失败 Trace | 在运行记录切换全部/成功/失败 | 数量与状态准确；成功 Trace 可打开详情 | P0 |
| TC-02B | M2A | 打开正常 Trace TRC-9f21 | 查看完整步骤 → 设为 Diff 基线 | 不显示诊断区；Diff 基线 ID 更新为 TRC-9f21 | P0 |
| TC-02C | M2/M2A | 注入不同业务步骤名、相同步骤类型的 Trace | 先按 `step_type` 筛选，再按 `step_name` 搜索 | 类型筛选跨业务命中；名称搜索只返回精确业务节点 | P0 |
| TC-02 | M2 | 已注入 20 条含 3 种已知模式的失败 Trace | 打开事故列表 | 生成 3 个失败模式簇，同根因聚簇率 ≥ 80% | P0 |
| TC-03 | M3 | 主线事故 INC-2026-0821-017 存在（FM-07，见 00 事实源） | 打开诊断工作台 | 一屏可见症状、Top-3 候选、因果路径；首屏 ≤ 3s | P0 |
| TC-04 | M3 | 同上 | 逐条点击所有证据引用 | 每条证据跳转到正确的原始事件，无死链 | P0 |
| TC-05 | M4 | 存在同类成功链 | 打开 Diff 视图 | 自动推荐基线并注明推荐依据；三维度差异高亮 | P0 |
| TC-06 | M5 | 打开候选根因 1 | 进入复核 → 确认是根因 | 复核落库，引导进入转用例流程 | P0 |
| TC-07 | M6 | 确认根因的事故 | 一键转回归用例 | ≤ 2 步完成；用例含输入、脱敏快照、不变量，字段自动填充 | P0 |
| TC-08 | M6 | 回归集含 REG-1041 | 用含已知回归的新版本触发门禁 | 门禁报警，不变量违反明细定位到具体字段 | P0 |
| TC-09 | 全链 | 干净环境 | 走通发现→诊断→复核→转用例全流程 | 主操作 ≤ 3 次（打开事故/复核/转用例） | P0 |

### 9.2 异常流程（对应任务 6）

| 编号 | 对应异常 | 前置条件 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-10 | D-1 | 注入 traceparent 丢失的 Trace | 打开该事故 | 断链警示 + 推测关联标记 + 修复指引可达 | P0 |
| TC-11 | D-2 | 插件与 SDK 双重上报 | 查看 Trace 与体检报告 | 展示层无重复 Span；体检指明重复来源；原始留档可查 | P0 |
| TC-12 | D-3 | 注入缺失 state 事件的 Trace | 打开诊断 | 状态类规则标注「数据不足」，不输出臆测候选 | P0 |
| TC-13 | D-4 | 注入 1500 节点 Trace，含 50KB 大字段 | 打开工作台与 Trace 树 | 聚合展示、按需加载；页面不卡死（交互响应 < 500ms） | P1 |
| TC-14 | D-5 | 输入含手机号/密钥的 Trace | 采集后查库与界面 | 原文不落库；界面显示掩码；体检记录拦截数 | P0 |
| TC-15 | G-1 | 注入规则与模型均无法判定的事故 | 打开诊断 | 显示「暂未定位」+ 相似历史事故；无编造结论 | P0 |
| TC-16 | G-2 | 所有候选均为证据不足 | 打开诊断 | 候选折叠为「线索」，复核入口禁用 | P1 |
| TC-17 | G-4 | 模拟模型超时 | 打开诊断 | 规则结果正常展示；模型区显示重试；页面不阻塞 | P0 |
| TC-18 | U-1 | 已确认是根因且已转用例 | 重新复核为「排除该原因」 | 复核历史保留；REG 用例标记「待复核」；校准信号更新 | P1 |
| TC-19 | U-2 | 两账号同时打开同一候选 | 双方先后提交不同复核 | 后提交者收到冲突提示（附议/重新复核），无静默覆盖 | P1 |
| TC-20 | U-3 | 以「分析师」角色登录 | 点击复核按钮 | 按钮禁用态 + 权限说明悬浮，无 5xx | P1 |
| TC-21 | U-4 | 新建空项目 | 打开各页面 | 各页面空状态引导正确，无报错、无假数据 | P1 |
| TC-28 | G-5 | 项目内无同类成功链 | 打开 Diff 视图 | 明示「无可比基线」并可放宽条件或跳过；不硬凑基线 | P1 |

### 9.3 边界条件

| 编号 | 类别 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| TC-22 | 空数据 | 事故列表筛选出 0 结果 | 空结果态 + 清除筛选入口 | P2 |
| TC-23 | 超大数据 | 单项目注入 10 万事故后打开列表 | 首屏 ≤ 2s；游标分页正确 | P1 |
| TC-24 | 边界值 | snapshot_age 恰为过期阈值 3600s | 规则判定行为与文档一致（≥ 阈值触发），不抖动 | P1 |
| TC-25 | 并发 | 100 并发 OTLP 写入 + 同时查询工作台 | 无写入丢失；查询可用 | P1 |
| TC-26 | 权限 | 直接以 URL 访问无权限项目的事故 | 403 且不泄露事故存在性 | P0 |
| TC-27 | 幂等 | 同一事故重复点击「转用例」 | 不生成重复用例，提示已存在 | P1 |

### 9.4 诊断质量专项（对应 AC3-1，验证方法承接第一周 10.2 节验证方式）

| 编号 | 内容 | 方法 | 通过标准 | 优先级 |
|---|---|---|---|---|
| TQ-01 | Top-3 命中率 | 30–50 条已知根因事故集（覆盖九类故障）跑批，按 00 事实源 0.7 节分桶统计 | 规则可判定类（FM-02/03/04）Top-3 ≥ 90%；混合类（FM-01/05/06）Top-3 ≥ 70%；语义复合类（FM-07/08/09）Top-3 ≥ 60%；不报单一合并命中率 | P0 |
| TQ-02 | 证据引用完整性 | 遍历全部上屏候选与证据 | 100% 有可解析 span_ref/event_ref；引用对象真实存在 | P0 |
| TQ-03 | 证据等级校准 | 按内部原始分数与证据等级统计确认率 | 等级间确认率应单调；上线前完成校准并记录版本 | P1 |
| TQ-04 | 反证有效性 | 人工抽检 30 条反面证据 | ≥ 60% 被评审人认为「有实质参考价值」 | P1 |
| TQ-05 | 规则/模型标签正确性 | 校验候选 source 标签与实际判定来源 | 100% 一致（规则结果不得标成模型，反之亦然） | P0 |

### 9.5 语义复合类诊断专项（FM-07/08/09，数据与编号见 00 事实源）

本节用例的验收对象是 **T2 诊断能力（设计中，尚未实现）**：本期用事实源中的案例快照预置为验收基准，T2 实现交付时按此跑批，不作为本期实测结论。证据编号 E1–E4、反证编号 C1–C2 均引用 00 事实源对应案例小节。

| 编号 | 类型 | 前置条件 | 步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TQ-06 | FM-07 正向 | 注入失败链 TRC-7e3d 与基线 TRC-2b98（INC-2026-0821-017） | 运行诊断 | 候选 1 为「检索排序漂移」，首故障点 S4；支持证据引用 E1（检索集 Diff，v5 被 top_k=3 截断）、E3（索引重建事件 2026-08-20 22:10）、E4（模型采信链），全部可回跳 | P0 |
| TQ-07 | FM-07 反证 | 注入 C1 对照链（同期 23 条召回 v3 但答复正确）与 C2 基线（重建前 30 天 top-1 稳定为 v5，采样 412 次） | 运行诊断并打开候选 1 复核页 | 23 条对照链不被误判为事故；反面证据区呈现 C1/C2；候选结论表述为复合条件（漂移 + 模型不校验 effective_date），不表述为单一条件必然失败 | P0 |
| TQ-08 | FM-08 正向 | 注入失败链 TRC-5c81 与基线 TRC-9a44（INC-2026-0822-024） | 运行诊断 | 候选 1 为「上下文多值冲突」，首故障点 S3；支持证据引用 E1（两地址值并存且均无 turn_index/supersedes）、E3（模型选中 token position 412 的 turn 1 旧值）、E4（工具入参与 turn 1 逐字一致），全部可回跳 | P0 |
| TQ-09 | FM-08 反证 | 注入 C2 对照链（同期 5 条单值改址链，无冲突值）并提供 C1 配置检查结果（v1.3.0–v1.4.0 无「最新优先」开关） | 运行诊断并打开候选 1 复核页 | 5 条对照链不被误判；反面证据区呈现 C1/C2；C1 用于排除「配置错误」替代解释，候选定性为能力缺失而非配置问题 | P0 |
| TQ-10 | FM-09 正向 | 注入失败链 TRC-3f6a 与基线 TRC-8d12（INC-2026-0823-031），含外部对账事件 | 运行诊断（无症状步骤，由对账事件触发） | 候选 1 为「非幂等写操作被超时重试」，首故障点 S6；支持证据引用 E1（req-a1f2 超时 / req-b7c3 成功两次调用）、E2（服务端 txn-9981 / txn-9987 双记录）、E3（无 idempotency_key）、E4（retry_on=[timeout] 且未声明 require_idempotent），全部可回跳 | P0 |
| TQ-11 | FM-09 反证 | 注入 C2 对照链（同期 8 条超时重试链，调用只读 order_api 与幂等 risk_check）并提供 C1 数据（近 7 天 47 次重试后成功） | 运行诊断并打开候选 1 复核页 | 8 条对照链不被误判；反面证据区呈现 C1/C2；修复建议方向为补幂等键 / 按工具幂等属性约束重试，不得建议全局禁用重试 | P0 |

---

## 交付闭环

| 检查项 | 结果 |
|---|---|
| 本任务产出 | 正向、异常、边界和诊断质量测试集 |
| 核验方式 | P0 验收标准均有用例；执行自动化尚未实现 |
| 当前状态 | 已完成 |
| 剩余风险 | 尚未接入 CI 执行器 |
