> 提交状态：已完成
> 核心交付：测试口径、覆盖率实测、P0 用例对照、E2E 结果
> 上游依据：second-week/docs/09（测试用例）· 下游证据：`npm run check` 可复现

# 任务 4：测试报告

## 4.1 总览（2026-08-31 V2 实测，`npm run check` 可复现）

| 套件 | 数量 | 结果 |
|---|---|---|
| server/tests（单元+API） | 92 | 全部通过 |
| sdk/tests | 8 | 全部通过 |
| agent/tests（工具+注入） | 4 | 全部通过 |
| e2e（离线态闭环冒烟） | 5 | 全部通过 |
| web（Vitest+RTL+MSW） | 7 | 全部通过 |
| **合计** | **116** | **全绿** |

较 V1 新增：4 项聚合多样性单测（FB-04 修复，见 [docs/06](06-反馈迭代闭环.md)）+
强化 e2e::test_04；对话演示 16 项（test_chat.py 13 + ChatPage.test.tsx 3，含多轮
--resume）；原型业务前端 11 项（test_protodata.py：data.js 生成器契约、node 语法
检查、静态托管、路径穿越防护、proto 与 midterm 逐字节一致性断言）。

## 4.2 覆盖率实测（pytest-cov，逐模块）

| 模块 | 覆盖率 | | 模块 | 覆盖率 |
|---|---|---|---|---|
| ingestion.py | 100% | | gate.py | 93% |
| clustering.py / db.py / enums.py | 100% | | pipeline.py | 93% |
| rules.py | 98% | | evaluator.py | 90% |
| diagnosis.py / causal.py / review.py | 97% | | api.py | 83% |
| diffgen.py | 94% | | sdk/parser.py | 95% |
| | | | server/xunji 合计 | **94%** |

核心判定模块（causal/rules/diffgen/diagnosis）全部 ≥94%，逐个达标，不靠平均掩盖。
未达 80% 的两个文件及理由：`sdk/cli.py 50%`（子进程包装主路径由真实录制验证，
见 fixtures/raw/）、`sdk/hook_reporter.py 0%`（hooks 旁路为附加接入方式，
未进入本期演示主路径——如实标注，列入复盘遗留）。

## 4.3 与 second-week/docs/09 P0 用例对照（实现范围内）

| docs/09 意图 | 本仓自动化测试 |
|---|---|
| 正向：失败事故产生 Top-3 候选与证据 | e2e::test_02/03/04、test_diagnosis::TestEndToEnd |
| 诊断质量：已知根因命中 | e2e::test_02（fetch_payments）/test_03（reconcile） |
| 证据引用完整性 100% | test_diagnosis::drops_candidate_without_evidence + DB CHECK |
| 异常：断链/重复/缺字段降级 | test_ingestion 三用例 |
| 异常：模型超时不阻塞（G-4） | test_diagnosis::model_failure_does_not_block |
| 异常：无基线冷启动 | test_diffgen::no_baseline、test_diagnosis::cold_start |
| 并发复核冲突 | test_review_gate::stale_if_match、test_api::409 |
| 误复核重做留审计 | test_review_gate::supersedes_previous |
| 门禁 warn/block/pass 三态 | test_review_gate 三用例 + e2e::test_05 |
| 边界：空数据/未知事件/非 JSON | test_sdk::tolerated、test_rules::clean_trace |

## 4.4 E2E 冒烟（离线态，机器证据）

`e2e/test_smoke_offline.py` 用真实录制 fixtures 走通
「入库 → 事故 → 诊断 → 复核 → 转用例 → 门禁(warn→pass)」，共 5 步断言；
命中实录（V2 3/3；V1 的 bad-tool-args 未命中记录如实保留）见 DEMO.md §5。

## 4.5 lint 与格式

- 后端/脚本：ruff 0 违例（`npm run check` 第一步）；
- 前端：TypeScript 严格模式 `tsc --noEmit` 0 错误（`npm run lint` in web/）。

---

| 检查项 | 结果 |
|---|---|
| 本任务产出 | 116 项测试、覆盖率实测、P0 对照、E2E 证据 |
| 核验方式 | `npm run check` + `cd web && npm test` 一键复现 |
| 当前状态 | 已完成 |
| 剩余风险 | hook_reporter 无自动化测试（未进演示主路径，已如实标注） |
